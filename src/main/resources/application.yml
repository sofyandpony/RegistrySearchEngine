## YAML Template.
server:
    address: localhost
    port: 8080

spring:
    profiles:
        active: development

logback:
    log:
        directory: /logs/tomcat/registry/searchEngine/
        logFileName: logs
        logFileError: logs_error
        fileDirectory: conf/logback.xml
